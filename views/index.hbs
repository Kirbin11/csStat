<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <meta charset="utf-8" />
</head>
<body>

    {{#each Matches}}
            <table align="left" border="1" cellpadding="0" cellspacing="1" style="height:206px; width:500px; margin:10px">
	        <caption>{{Match}}</caption>
	            <tbody>
                    {{#each MapsBets}}
                        <tr>
                        <td style="padding: 5px">{{someMap}}</td>
                        <td style="padding: 5px">{{someBet}}</td>
                        </tr>
                    {{/each}}
	            </tbody>
    {{/each}}
</table>

</body>
<html>